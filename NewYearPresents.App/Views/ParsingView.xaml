<UserControl x:Class="NewYearPresents.App.Views.ParsingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NewYearPresents.App.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" >
        <Button
            Width="180" Content="Загрузить продукты"
            Height="50" Margin="0 10"
            Background="White" BorderBrush="Black"
            Foreground="Black" Command="{Binding Path=ParseProductsCommand}" />
        <Button
            Width="180" Content="Загрузить упаковку"
            Height="50" Margin="0 10"
            Background="White" BorderBrush="Black"
            Foreground="Black" Command="{Binding Path=ParsePackagingsCommand}" />
        </StackPanel>
    </Grid>
</UserControl>