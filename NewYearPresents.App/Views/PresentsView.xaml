<UserControl x:Class="NewYearPresents.App.Views.PresentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NewYearPresents.App.Views"
             xmlns:converter="clr-namespace:NewYearPresents.App.Core.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Name="OrderUC">
    <UserControl.Resources>
        <converter:MathConverter x:Key="MathConverter"></converter:MathConverter>
    </UserControl.Resources>

    <Grid>
        <StackPanel 
            Orientation="Vertical" HorizontalAlignment="Left"
            Width="{Binding ElementName=OrderUC,
            Path=ActualWidth,
            Converter={StaticResource MathConverter},
            ConverterParameter=(@VALUE*2/3)}">
            <ListBox>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button Width="200" Height="200">
                            <Button.Content>
                                <Image Source="{Binding PackagingImage}"/>
                            </Button.Content>
                        </Button>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
